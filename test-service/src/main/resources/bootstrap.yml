spring:
  application:
    name: test-spring
  cloud:
    nacos:
      server-addr: 192.168.200.104:8848
      config:
        file-extension: yml
        group: DEFAULT_GROUP
      discovery:
        server-addr: 192.168.200.104:8848
  profiles:
    active: dev
  # main:
    #allow-bean-definition-overriding: false # 允许存在个多Feign